{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "ConnectionStrings": {
    "redis": "localhost:6379",
    "default": "Server = .; Integrated Security = True; Database = BookingClone ; TrustServerCertificate = True;"
  },

  // All the rest in .env due to secrets contained
}
